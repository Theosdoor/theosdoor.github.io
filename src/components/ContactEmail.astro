---
import { contactConfig } from '../utils/constants';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={['inline-flex items-center', className]}>{contactConfig.emailObfuscatedText}</span>
