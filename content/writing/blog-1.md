---
title: "Blog 1"
date: "2026-08-30"
summary: "One-line teaser shown on the /writing/ index. Optional."
draft: true
---

Drafts (`draft: true`) are skipped at build time, so this file will not appear on
the site until you flip it to `false` — copy it as the starting point for a post.

Write in plain Markdown: Obsidian wikilinks (`[[note]]`) and embeds (`![[img]]`)
are not rendered, so use `[text](https://example.com)` for links.
