---
title: "Test post: what the writing index looks like"
date: "2026-08-31"
summary: "A throwaway post so the new index layout has something to render — image, title, subtitle and date, Substack style. Delete before merging."
image: "/images/writing/test-post.png"
---

This post only exists to preview the `/writing/` index layout. Delete this file
and `public/images/writing/test-post.png` before merging.

The `image` field is optional — posts without one render as a full-width row.
